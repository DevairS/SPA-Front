import styled from 'styled-components';
import { Snackbar as _Snackbar } from '@material-ui/core';
import { Alert as _Alert } from '@material-ui/lab';

export const Wrapper = styled(_Snackbar)``;

export const Alert = styled(_Alert)``;
